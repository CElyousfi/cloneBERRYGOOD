/* Migré depuis public/app.jsx — extraction verbatim (non-régression).
   Module: caisse | Déclaration(s): isModeFacilite */


function isModeFacilite(mode) { return mode === 'facilite' || mode === 'comptant'; }

export { isModeFacilite };
